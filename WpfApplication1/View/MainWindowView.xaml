<Window x:Class="P2P_Netwerken.View.MainWindowView"
        x:Name="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:P2P_Netwerken.View"
        mc:Ignorable="d"
        Title="Home" Height="450" Width="800">
    <Grid>
        <Button Name="SearchPeersButton" Content="Search for peers" HorizontalAlignment="Center" Width="184" Height="45" Command="{Binding SearchForPeersButtonClickCommand}" CommandParameter="{Binding ElementName=MainWindow}" Margin="0,-100,0,0" />
        <Button Content="Manual chatbox" HorizontalAlignment="Center" VerticalAlignment="Top" Width="184" Command="{Binding ChatButtonClickCommand}" CommandParameter="{Binding ElementName=MainWindow}" Height="45" Margin="0,209,0,0" />

    </Grid>
</Window>
